# Changelog

## v1.1 — 2026-07-11

안정성과 마감 손질에 집중한 첫 마이너 업데이트.

### 새 기능
- **읽기 전용 뷰어 모드** — 세 에디터(시나리오/마인드맵/페이지) 툴바에 잠금 토글. 잠그면 입력기·인스펙터 편집·실행취소·AI·가져오기가 모두 닫히고, 스크롤·탭 전환·분기 열람·팬줌·내보내기는 그대로 동작. 탭(세션) 단위로 켜고 끈다.
- **리허설 모드** 🎬 — 읽기 전용 상태의 시나리오를 ▶︎ 버튼으로 재생하면 블록이 실제 대화처럼 하나씩 등장한다. 대사 길이에 비례한 타이밍, 화자 이름이 붙은 '입력 중…' 인디케이터, 자동 스크롤, 일시정지/정지/0.5×~2× 속도 조절.
- **PDF 다중 페이지 분할** — 페이지 Export PDF가 한 장짜리 긴 페이지 대신 블록 높이를 측정해 A4 페이지로 나눠 담는다. 여러 장일 때 'n / N' 푸터. PDFKit 기반 테스트 추가.
- **작가 프로필 사진 원형 크롭** — 설정에 '크롭 조정…' 시트(드래그 팬 + 줌 슬라이더). 크롭 결과가 설정·프로필 페이지·사이드바 아바타에 일관 반영. 캐릭터 크롭 UI를 공용 컴포넌트(CircularCropEditor)로 추출해 함께 사용.

### 안정성
- NSPopover 앵커 크래시 하드닝 마무리 — 블록 행 미디어 뷰 수정(1.0.x)을 머지하고, 전 popover/sheet 호출부를 전수 점검해 캐릭터 갤러리 확대 시트의 동일 패턴까지 지연 처리.
- 읽기 전용 세션은 `markDirty` 차원에서도 보호 — 우회 변경이 저장을 더럽힐 수 없다.

### 개발 체계
- swiftformat/swiftlint 설정 도입 — 기존 스타일(4칸 들여쓰기, 무공백 레인지, no-indent `#if`)에 맞춘 보수적 규칙. 1회성 위생 정리로 두 린터 모두 0건 통과 상태에서 시작.
- 실제 지적 사항 수리: 기여도 그래프의 캘린더 강제 언래핑 제거, for-where 정리 등.

### 참고
- Developer ID 서명/노터라이제이션은 유료 Apple Developer 멤버십이 필요해 이번 버전에서 보류 (현재 무료 계정).

## v1.0 — 2026-07-09

첫 배포 버전. 3종 에디터와 프로젝트 관리 기능의 기본기를 갖췄다.

### 에디터
- **시나리오(.scen)** — 대사/지침 블록, 다중 화자 ⌘선택, 드래그 정렬, 캐릭터 인스펙터, AI 제안 수락/무시
- **시나리오 분기** — 블록 우클릭 '여기서 분기 만들기', 툴바 분기 피커(본편/분기 전환), 분기점 컨텍스트 표시, AI 이어쓰기가 활성 분기 흐름을 따름
- **마인드맵(.scno)** — 팬/줌, 더블클릭 노드 생성, 연결선+캡션, 노드 인스펙터, 이미지 노드
- **블록 페이지(.scpa)** — `/` 커맨드, Enter 분할, 빈 블록 Backspace 병합, Tab 들여쓰기, 토글 접기, Markdown Import/Export
- **캐릭터 페이지 v2** — 탭 구조(프로필/노트/관계/갤러리/보이스), 구조화 필드, 프로필 이미지 팝업 패널(원형 크롭 통합), 방사형 관계도, 갤러리+시점 태그, 선택적 보이스 카드(AI 말투 주입)
- **캐릭터 락업 양방향** — 캐스트→캐릭터 페이지 생성 + 프로젝트 캐릭터 페이지→캐스트 가져오기(프로필 승계)
- **페이지 이미지/표 블록** — 파일 선택·드래그 앤 드롭·URL 임베드, 비율 지정(원본/16:9/4:3/1:1), 클릭 확대, 편집형 표(행/열 추가·삭제), Markdown 왕복 변환
- **페이지 Export** — Markdown + HTML(이미지 base64 임베드) + PDF

### 프로젝트 관리
- **참조 패널** — 문서별 속성/참조(추가·제거·열기)/백링크, 저장 시 캐스트·마인드맵 링크에서 자동 참조 파생
- **본문 검색** — SQLite 색인에 평문 포함, 홈 검색이 제목+본문 딥서치
- **파일 아카이브** — 카테고리/정렬/리스트·그리드, 가리기·휴지통(Touch ID 게이트, Finder 반영)
- **저장** — 자동(디바운스)/수동(⌘S), 4색 상태 배지, 종료 시 자동 백업 + 타임라인 복원
- **설정** — 4탭, 언어(한/일/영) 즉시 전환, 강조색 5종+커스텀, 품질 3단계, 배경 파라미터
- **AI 에이전트 채팅** — 전용 탭 + 사이드패널 미니 챗, 제공자 3종(Apple 온디바이스/Anthropic/오프라인) 공용
- **단축키** — ⌘T/⇧⌘A/⌘W/⌘S/⌘1~9, 슬래시 메뉴 ↑↓·Enter
- **Touch Bar 지원(베타)** — 설정 토글 + 기능 목록/프리뷰 + 실제 NSTouchBar 배선

### 디자인 & 성능
- Sonnet 테마(앤티크 페이퍼 + 적갈 액센트, Pretendard 기본), 깃털 앱 아이콘
- Chrome-tabs 풀폭 탭바(캡슐 옵션), 픽셀 브리딩 필드(홈/사이드바), Liquid Glass 끄기 기본
- 전체화면/윈도우 레이아웃 분기 — 헤더·픽셀 필드가 각각 다른 위치·크기로 렌더링
- 모션 패스 — 카드 호버 리프트+눌림, 저장 배지 펄스, 사이드바 탭 슬라이딩 하이라이트, AI 채팅 메시지 도착 트랜지션
- Wavy Dot Field — Metal 셰이더, 셰이더 부재 시 Canvas 폴백

### 배포
- macOS DMG 패키징 스크립트(`Scripts/build-dmg.sh`), 튜토리얼 프로젝트 자동 생성 도구(`Scripts/DemoProjectGenerator`)
- MIT 라이선스, `.gitignore` 정리

## 다음 단계

- CloudKit 동기화 (별도 릴리스 규모)
- App Intents(Siri/Apple Intelligence 노출), PCC 제공자
- 인라인 @멘션 — 참조 패널 사용성 검증 후 판단
- Developer ID 서명 + 노터라이제이션 — 유료 멤버십 확보 시 (현재는 애드혹 서명, Gatekeeper 경고 후 실행 필요)
- 페이지 에디터 커스텀 드래그 재정렬 복원 (List→LazyVStack 전환으로 유실)
