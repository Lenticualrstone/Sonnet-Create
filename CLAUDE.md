# Remote access

This project is meant to stay mirrored on GitHub (`origin/main`,
`Lenticualrstone/Sonnet-Create`) so it can be opened from claude.ai/code on
any device. When wrapping up a session with real changes, offer to commit
and push them to keep the remote copy current.
